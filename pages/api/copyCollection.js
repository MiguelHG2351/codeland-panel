// import userModel from '../../models/users'
// import userTestModel from '../../models/usersTest'

export default async  (req, res) => {
    // const getUsers = await userModel.find({}).lean()
    // const setUsers = await userTestModel.insertMany(getUsers, (err, docs) => {
    //     if (err) {
    //         console.log(err)
    //     }
    //     return res.send(docs)
    // })
    // console.log(setUsers)
    
    res.send('Hello word')
}